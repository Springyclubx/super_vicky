/// Default class for use on [tryFormatDate] or [tryParseDate]
class FormatDate {
  /// Default format date month year or (dd/MM/yyyy)
  static const dateMonthYear = 'dd/MM/yyyy';

  /// Default format hour minute or (HH:mm)
  static const hourMinute = 'HH:mm';
}
